<table class="table">
  <tr>
    <th></th>
    <th>name</th>
    <th>create at</th>
    <th>update at</th>
  </tr>
  <% items.each do |item| %>
    <tr>
      <td><span class="download"><%= image_tag "ios7-download-outline.png" %></span></td>
      <td><%= link_to item.relative_path, item %></td>
      <td><%= item.created_at.to_s %></td>
      <td><%= item.updated_at.to_s %></td>
    </tr>
  <% end %>  
</table>
